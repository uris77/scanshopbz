// ignore: always_declare_return_types
import 'package:test/test.dart';

// ignore: always_declare_return_types
main() {
  group('Prices', () {
    group('persistence', () {
      test('should save a product`s price', () {});
    });
    group('querying', () {
      test('should query all prices for a product', () {});
      test('should query all prices for a product in a store', () {});
    });
  });
}
